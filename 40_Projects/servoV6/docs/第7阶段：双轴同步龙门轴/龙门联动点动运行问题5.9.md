# 龙门联动点动运行问题5.9

## 问题描述

当前运行步骤为：

- 运行进程
- 切换到双轴
- 打开联动耦合选项
- X逻辑轴点动前进

但是此时X逻辑轴并没有运动。查看日志：

```bash
qml: UI 路由切换至: Gantry-A [龙门模式]
[10:53:05.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Decoupled
[10:53:06.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Decoupled
[10:53:07.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Decoupled
[10:53:08.286][INFO][UI][GantryVM][N/A][N/A][N/A] requestCoupling -> ACCEPTED
[10:53:08.293][SUMMARY][DOM][GantryVM][N/A][N/A][N/A] Mode changed: Decoupled -> Coupled
[10:53:08.293][INFO][DOM][GantryEvent][N/A][N/A][N/A] Event: Coupling requested
[10:53:08.293][INFO][DOM][GantryEvent][N/A][N/A][N/A] Event: Coupled mode entered
[10:53:08.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Coupled
[10:53:09.743][INFO][UI][GantryVM][N/A][N/A][N/A] jogCoupledForward -> ACCEPTED
[10:53:09.773][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Coupled
[10:53:10.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Coupled
[10:53:11.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Coupled
[10:53:12.709][INFO][UI][GantryVM][N/A][N/A][N/A] jogCoupledRelease - stop sent to logical axis X
[10:53:12.772][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Coupled
```

可以看出成功调用了 jogCoupledForward ，但是位置并没有变化。

## 糟心的日志系统改善

这时候我发现龙门结构的日志系统存在问题。因为正常的单轴运行日志为：

```bash
[11:00:01.615][INFO][APP][JogOrch][G1][Y][T-240] START Jog direction=Forward(+)
[11:00:01.623][TRACE][HAL][Driver][G1][Y][T-240] Sending to PLC: EnableCommand(active=true)
[11:00:01.773][DEBUG][APP][JogOrch][G1][Y][T-240] Step: EnsuringEnabled -> IssuingJog
[11:00:01.785][TRACE][HAL][Driver][G1][Y][T-240] Sending to PLC: JogCommand(dir=Forward, active=true)
[11:00:01.785][DEBUG][APP][JogOrch][G1][Y][T-240] Step: IssuingJog -> Jogging
[11:00:02.014][TRACE][HAL][System][N/A][N/A][N/A] Tick: X1.pos=0.000000 X2.pos=0.000000 X1.isEnabled=1 X2.isEnabled=1 Mode=Decoupled
[11:00:02.317][INFO][UI][AxisVM][G1][Y][T-9610] User RELEASED Jog Positive (+)
[11:00:02.317][INFO][APP][JogOrch][G1][Y][T-9610] Stop Jog Requested by UI
[11:00:02.323][TRACE][HAL][Driver][G1][Y][T-240] Sending to PLC: JogCommand(dir=Forward, active=false)
[11:00:02.323][DEBUG][APP][JogOrch][G1][Y][T-240] Step: IssuingStop -> WaitingForIdle
[11:00:02.334][DEBUG][APP][JogOrch][G1][Y][T-240] Step: WaitingForIdle -> EnsuringDisabled
[11:00:02.344][TRACE][HAL][Driver][G1][Y][T-240] Sending to PLC: EnableCommand(active=false)
[11:00:02.354][SUMMARY][APP][JogOrch][G1][Y][T-240] Jog Operation -> SUCCESS (Safely Stopped)
```

可以看出单轴日志里有 `[G1][Y][T-240]` 这样的上下文追踪，而双轴全是 `[N/A][N/A][N/A]`，这让排错变得困难。

试着改一下双轴龙门的日志系统。

我们看下成功调用的代码：

```C++
void GantryViewModelCore::jogCoupledForward() {
    auto result = m_gantry.jog(AxisId::X, MotionDirection::Forward);
    logCommandResult("jogCoupledForward", result);
}
```

jog() 函数实现为：

```c++
/**
 * @brief Jog 命令
 *
 * Coupled 模式: 命令写入 LogicalAxis X 的命令槽
 * Decoupled 模式: 命令写入对应 PhysicalAxis 的活跃标记
 */
CommandResult jog(AxisId target, MotionDirection dir) {
    // Jog 可以覆盖正在执行的 Jog (约束 19: TC-6.6)
    // 先清除命令槽中的 Jog，再走正常的操作检查流程
    if (::isCoupled(m_mode) || target == AxisId::X) {
        if (m_logical.pendingCommand().type == LogicalAxis::CommandType::Jog) {
            m_logical.clearCommand();
        }
    }

    auto op = checkOperability(target, dir);
    if (op != Operability::Allowed) {
        auto event = GantryEvents::Event::commandRejected(operabilityToString(op));
        m_events.push_back(event);
        return {false, operabilityToString(op), event};
    }

    // 写入命令槽
    if (::isCoupled(m_mode)) {
        std::string err = m_logical.tryAcceptJog(dir);
        if (!err.empty()) {
            auto event = GantryEvents::Event::commandRejected(err);
            m_events.push_back(event);
            return {false, err, event};
        }
    }
    // Decoupled 模式下直接将命令下发给物理轴（此处仅做记录）
    // 实际命令拆分由 DomainService 负责

    return {true, "", GantryEvents::Event{}};
}
```

