以下为 `GantryMotorController` TDD 完成后，剩余需要修改的全部文件及修改内容。

---

# 剩余修改清单

## 已完成（无需再动）

| #    | 文件                                                   | 状态                                          |
| ---- | ------------------------------------------------------ | --------------------------------------------- |
| 1    | `domain/gantry/GantryFeedback.h`                       | ✅ 已存在                                      |
| 2    | `domain/gantry/GantryGroup.h`                          | ✅ 已引用新 DTO                                |
| 3    | `domain/gantry/GantryMotorController.h`                | ✅ 五态状态机                                  |
| 4    | `domain/gantry/GantryRejection.h`                      | ✅ 含 StateConflict(100)、NotSynchronized(102) |
| 5    | `tests/domain/gantry/test_gantry_motor_controller.cpp` | ✅ 21 个用例                                   |

---

## 待修改文件（共 9 个）

### 文件 1：`domain/entity/SystemContext.h`

**修改内容**：注入 `GantryMotorController`

```cpp
// 新增 include
#include "gantry/GantryMotorController.h"

class SystemContext {
public:
    // 现有接口
    GantryGroup& gantry() { return *m_gantry; }

    // ─── 新增：电机控制器访问接口 ───
    GantryMotorController& gantryMotor() { return *m_gantryMotor; }

private:
    std::unique_ptr<GantryMotorController> m_gantryMotor;  // 新增
    std::unique_ptr<GantryGroup> m_gantry;                 // 现有
    // ...
};

// 构造函数中新增：
// m_gantryMotor = std::make_unique<GantryMotorController>();
```

---

### 文件 2：`application/policy/GantryOrchestrator.h`

**修改 2.1**：移除 `tryGetAxis(AxisId::X)` 死锁根源

```cpp
void tick() {
    SystemContext* group = nullptr;
    ContextRejection mgrReason = ContextRejection::None;
    if (!m_manager.tryGetGroup(m_groupName, group, mgrReason)) {
        m_step = Step::Error;
        m_lastError = mgrReason;
        return;
    }

    // ❌ 删除以下 5 行（悖论根源）：
    // Axis* x = nullptr;
    // ContextRejection ctxReason = ContextRejection::None;
    // if (!group->tryGetAxis(AxisId::X, x, ctxReason)) { ... }

    // ✅ 改为直接获取两个领域对象
    GantryGroup& gantry = group->gantry();
    GantryMotorController& motor = group->gantryMotor();

    switch (m_step) { ... }
}
```

**修改 2.2**：`EnsuringEnabled` 步骤

```cpp
case Step::EnsuringEnabled: {
    // ✅ 直接通过电机控制器下发使能，不经过 EnableUseCase
    auto result = motor.requestEnable(true);
    if (result == GantryRejection::None) {
        // 若有 pending 命令，下发到驱动
        if (motor.hasPendingCommand()) {
            if (auto* drv = group->driver()) {
                auto cmd = motor.popPendingCommand();
                drv->sendGantryMotor(cmd);
            }
        }
        m_step = Step::WaitingEnabled;
    } else {
        m_step = Step::Error;
        m_lastError = result;
    }
    break;
}
```

**修改 2.3**：`WaitingEnabled` 步骤

```cpp
case Step::WaitingEnabled:
    // ✅ 改用 motor.isEnabled() 判断，不再依赖 x->state() == AxisState::Idle
    if (motor.isEnabled()) {
        m_step = Step::Coupling;
    }
    break;
```

**修改 2.4**：`lastError` 类型变更

```cpp
// 旧：
// std::variant<std::monostate, ContextRejection, GantryRejection> m_lastError;

// 新：
std::variant<std::monostate, ContextRejection, GantryRejection> m_lastError;
// （不变，但需确认 union 包含 GantryRejection）
```

---

### 文件 3：`infrastructure/ISystemDriver.h`

**修改内容**：新增龙门电机命令通道

```cpp
#include "domain/gantry/GantryMotorController.h"

class ISystemDriver {
public:
    virtual ~ISystemDriver() = default;

    virtual void send(AxisId id, const AxisCommand& cmd) = 0;       // 现有
    virtual void sendGantry(const GantryCommand& cmd) = 0;          // 现有

    // ─── 新增 ───
    virtual void sendGantryMotor(const MotorCommand& cmd) = 0;
};
```

---

### 文件 4：`infrastructure/FakeAxisDriver.h`

**修改内容**：实现 `sendGantryMotor`

```cpp
void sendGantryMotor(const MotorCommand& cmd) override {
    // 龙门电机使能 = 同时使能 X1/X2
    plc.onCommand(AxisId::X1, EnableCommand{cmd.enable});
    plc.onCommand(AxisId::X2, EnableCommand{cmd.enable});
}
```

---

### 文件 5：`infrastructure/FakePLC.h`

**修改内容**：新增 `getGantryFeedback()` 方法

```cpp
#include "domain/gantry/GantryFeedback.h"

GantryFeedback getGantryFeedback() const {
    bool x1Enabled = (m_axes.at(AxisId::X1).feedback.state != AxisState::Disabled);
    bool x2Enabled = (m_axes.at(AxisId::X2).feedback.state != AxisState::Disabled);

    return GantryFeedback{
        .enable    = (x1Enabled && x2Enabled),
        .isCoupled = false,   // 初期简化，后续可扩展
        .errorCode = 0
    };
}
```

---

### 文件 6：`tests/domain/gantry/test_gantry_group.cpp`

**修改内容**：所有 `GantryFeedback` 构造语句加入 `.enable` 字段

```cpp
// 旧：gantry->applyFeedback({ .isCoupled = true, .errorCode = 0 });
// 新：
gantry->applyFeedback({ .enable = false, .isCoupled = true, .errorCode = 0 });
```

逐行替换所有 `applyFeedback` 调用，补上 `.enable = false`（或 `true`，按场景）。

---

### 文件 7：`tests/application/policy/test_gantry_orchestrator.cpp`

**修改 7.1**：SetUp 适配新接口

```cpp
class GantryOrchestratorTest : public ::testing::Test {
protected:
    void SetUp() override {
        ContextRejection reason;
        manager.createGroup("TestGroup", reason);

        SystemContext* ctx = nullptr;
        ContextRejection mgrReason;
        manager.tryGetGroup("TestGroup", ctx, mgrReason);
        ctx->setDriver(&driver);

        // ✅ 不再获取 Axis* x
        gantry = &ctx->gantry();
        motor  = &ctx->gantryMotor();  // 新增

        orchestrator = std::make_unique<GantryOrchestrator>(manager, "TestGroup");
    }

    GantryMotorController* motor = nullptr;  // 新增成员
};
```

**修改 7.2**：修复现有 5 个测试（用 `motor` 替代 `xAxis` 判断）

| 旧                               | 新                                       |
| -------------------------------- | ---------------------------------------- |
| `x->state() == AxisState::Idle`  | `motor.isEnabled()`                      |
| `x->state() == AxisState::Error` | 移除（电机控制器不暴露 Error，超时可选） |

**修改 7.3**：新增 3 个悖论验证测试

| 测试名称                                    | 验证点                              |
| ------------------------------------------- | ----------------------------------- |
| `coupling_does_not_deadlock_when_decoupled` | Decoupled 下流程不进入 Error        |
| `coupling_fails_when_not_synchronized`      | 未同步 → Error + NotSynchronized    |
| `full_coupling_flow_from_decoupled`         | 从 Decoupled + 未使能 → Done 全链路 |

---

### 文件 8：`tests/CMakeLists.txt`

**修改内容**：注册新测试可执行文件

```cmake
add_executable(test_gantry_motor_controller
    domain/gantry/test_gantry_motor_controller.cpp
)
target_link_libraries(test_gantry_motor_controller gtest_main)
add_test(NAME test_gantry_motor_controller COMMAND test_gantry_motor_controller)
```

---

### 文件 9（可选）：`domain/entity/SystemContext.h` 中移除 `m_isGantryCoupled` 硬编码

如果 `SystemContext` 仍有 `bool m_isGantryCoupled` 硬编码，应移除。联动状态由 `GantryGroup` 负责。

---

## 总结

```
已完成：5 个文件
待修改：9 个文件
```

核心变更链：`GantryMotorController` → `SystemContext` 注入 → `GantryOrchestrator` 解耦 → 基础设施扩展 → 测试适配。