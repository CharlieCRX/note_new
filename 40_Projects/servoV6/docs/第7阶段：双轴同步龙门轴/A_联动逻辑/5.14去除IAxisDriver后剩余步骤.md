Now I have a thorough understanding of the codebase. Let me present the comprehensive adjustment plan.

---

## 具体调整方案

基于文档"统一命令总线"架构，在已完成步骤（1~5）的基础上，还需要修改以下文件。以下是**逐文件的具体调整清单**：

---

### 一、UseCase 层 — `send(axisId, AxisCommand)` → `send(SystemCommand)`

当前 5 个 UseCase 文件都调用 `drv->send(axisId, axis->getPendingCommand())`（双参数签名），需要改为打包成 `SystemCommand` 后调用单参数 `send()`。

#### 1. `application/axis/EnableUseCase.h`
**当前（阶段3）:**
```cpp
if (auto* drv = group->driver()) {
    drv->send(axisId, axis->getPendingCommand());
}
```
**改为:**
```cpp
if (auto* drv = group->driver()) {
    drv->send(SystemCommand{AxisCommandWithId{axisId, axis->getPendingCommand()}});
}
```
**需要新增 `#include "domain/command/SystemCommand.h"`**

---

#### 2. `application/axis/MoveAbsoluteUseCase.h`
**同 EnableUseCase**，阶段3 的 `drv->send(axisId, ...)` 改为 `drv->send(SystemCommand{AxisCommandWithId{axisId, ...}})`，新增 include。

---

#### 3. `application/axis/MoveRelativeUseCase.h`
**同上。**

---

#### 4. `application/axis/JogAxisUseCase.h`
**两处 send 调用**（`execute` 和 `stop` 方法）都需要修改，同上模式。

---

#### 5. `application/axis/StopAxisUseCase.h`
**同上。**

---

### 二、Policy 层 — `sendGantry(...)` → `send(SystemCommand)`

#### 6. `application/policy/GantryOrchestrator.h`

**当前有两处 `drv->sendGantry(cmd)`：**
```cpp
// Coupling 步骤
drv->sendGantry(cmd);

// Decoupling 步骤
drv->sendGantry(cmd);
```

**改为:**
```cpp
drv->send(SystemCommand{cmd});
```

**需要新增 `#include "domain/command/SystemCommand.h"`**

此外，`group->gantry()` 调用需要确认与 `SystemContext.h` 保持一致。当前 `SystemContext` 暴露的是 `gantryCouplingController()` 和 `gantryPowerController()`，而 `GantryOrchestrator` 使用 `group->gantry()` 返回 `GantryGroup&`。如果当前 `SystemContext` 没有 `gantry()` 方法，则该调用也需要调整为通过 `gantryCouplingController()` 访问（或由你在确认方案时决定如何处理）。

---

#### 7. `application/policy/AutoAbsMoveOrchestrator.h`
需要检查是否引用了 `IAxisDriver` 或调用了 `send(axisId, ...)`，如有则同模式修改。

#### 8. `application/policy/AutoRelMoveOrchestrator.h`
同上。

#### 9. `application/policy/JogOrchestrator.h`
同上。

---

### 三、Domain 层

#### 10. `domain/entity/SystemContext.h`
当前已使用 `ISystemDriver*`（不是 `IAxisDriver*`），**无需修改**。但如果缺少 `gantry()` 方法（返回 `GantryGroup`），GantryOrchestrator 需要同步适配。

---

### 四、测试文件 — `IAxisDriver` 引用 → `ISystemDriver`

所有测试文件中直接引用 `IAxisDriver` 的地方需要改为 `ISystemDriver`。

#### 11. `tests/application/test_system_manager.cpp`
需检查是否有 `#include "application/axis/IAxisDriver.h"` 和对 `IAxisDriver` 的引用，改为 `ISystemDriver`。

#### 12. `tests/infrastructure/test_system_integration.cpp`
需检查并替换 `IAxisDriver` → `ISystemDriver`。

#### 13. 其他测试文件
以下测试文件可能间接引用或使用 FakeAxisDriver / IAxisDriver：
- `tests/application/test_enable_usecase.cpp`
- `tests/application/test_jog_usecase.cpp`
- `tests/application/test_move_absolute_usecase.cpp`
- `tests/application/test_move_relative_usecase.cpp`
- `tests/application/test_stop_usecase.cpp`
- `tests/application/policy/test_gantry_orchestrator.cpp`
- `tests/application/policy/test_auto_abs_move_orchestrator.cpp`
- `tests/application/policy/test_auto_rel_move_orchestrator.cpp`
- `tests/application/policy/test_jog_orchestrator.cpp`

每个文件检查：
1. `#include "application/axis/IAxisDriver.h"` → 删除（因为 IAxisDriver.h 已删除）
2. `IAxisDriver*` 类型引用 → `ISystemDriver*`
3. `drv->send(axisId, cmd)` 双参数调用 → `drv->send(SystemCommand{AxisCommandWithId{axisId, cmd}})`
4. `drv->sendGantry(cmd)` → `drv->send(SystemCommand{cmd})`

---

### 五、CMakeLists.txt

#### 14. `domain/CMakeLists.txt`
需新增 `command/SystemCommand.h` 到头文件列表。

#### 15. `tests/CMakeLists.txt`
如果引用了 `application/axis/IAxisDriver.h`（已删除），需移除。

---

### 六、其他潜在引用

#### 16. `application/SystemManager.h`
可能引用了 `IAxisDriver`，需改为 `ISystemDriver`。

#### 17. `application/axis/AxisSyncService.h`
可能引用了 `IAxisDriver`，需检查。

---

### 改动汇总

| 序号  | 文件                                           | 改动类型                                                     |
| ----- | ---------------------------------------------- | ------------------------------------------------------------ |
| 1     | `application/axis/EnableUseCase.h`             | `send(axisId, cmd)` → `send(SystemCommand{...})` + include   |
| 2     | `application/axis/MoveAbsoluteUseCase.h`       | 同上                                                         |
| 3     | `application/axis/MoveRelativeUseCase.h`       | 同上                                                         |
| 4     | `application/axis/JogAxisUseCase.h`            | 同上（两处）                                                 |
| 5     | `application/axis/StopAxisUseCase.h`           | 同上                                                         |
| 6     | `application/policy/GantryOrchestrator.h`      | `sendGantry` → `send(SystemCommand{...})` + include；`gantry()` 适配 |
| 7     | `application/policy/AutoAbsMoveOrchestrator.h` | IAxisDriver 引用检查                                         |
| 8     | `application/policy/AutoRelMoveOrchestrator.h` | IAxisDriver 引用检查                                         |
| 9     | `application/policy/JogOrchestrator.h`         | IAxisDriver 引用检查                                         |
| 10    | `domain/entity/SystemContext.h`                | 确认 `gantry()` 方法存在                                     |
| 11~13 | 测试文件（约9个）                              | `IAxisDriver` → `ISystemDriver` + 调用签名适配               |
| 14    | `domain/CMakeLists.txt`                        | 新增 `SystemCommand.h`                                       |
| 15    | `tests/CMakeLists.txt`                         | 移除 IAxisDriver.h 引用                                      |

---

**核心原则**：所有调用侧统一走 `ISystemDriver::send(SystemCommand)`，不再区分 `send(axisId, axisCmd)` 和 `sendGantry(gantryCmd)` 两条路径。

请确认以上方案，我立即开始逐一修改。