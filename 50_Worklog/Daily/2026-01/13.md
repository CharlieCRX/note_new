# 1.13

- 初步测试统一的错误处理入口
- 点动开始阶段发生位置超差后，松开按钮，就会禁用所有按钮
  - UI 解锁只依赖 `on_jogDone`
  - `disableAllbt()` 调用时机错误。`canStartJog()` 只是 *“是否允许启动”* 的判断
  - `on_*_released()` 在“错误态”下再次禁用 UI：“后面一定会有 `on_jogDone` 来解锁”
- 正确的应该是：在按钮按下的那一刻，就应该禁用所有操作，直到点动流程以任意方式结束
- 加入了“点动事务”的逻辑统一管控点动相关的 UI 以及配置等
- 小心翼翼地删除杂糅在点动流程中的点动事务:cry:
- 下午4点水池空出来了，要给水池升级软件了
- `checkJog`中涉及到的通过速度检测是否为0来判断到达物理限位是存在问题的！
- 现在对于点动业务的阶段处理，仅是处理禁止和松开按钮
- 处理完毕后，开始着手于`m_allDone` 和 `m_jog_done`的全局变量分析
- 将两个全局变量替换为了更为明晰的名称