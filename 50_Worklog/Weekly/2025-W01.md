# 第一周总结

## 工作

- 重构了`canStartJog()`的代码
  - 将“数据采集”与“逻辑判断”彻底解耦，解决了多次重复读取同一个寄存器导致的延迟较大问题
  - 分离业务逻辑逻辑层深度依赖的底层驱动接口，方便后续架构升级
- 分析`checkJog`的业务逻辑复杂性，并记录在文档中，方便后续重构流程
- 针对`checkJog`中糅杂的 if/break 构建起来的重复中断点动的逻辑流，加入StopReason 模型将电机停转的原因抽象为业务信息
- 不需要再复制 stop + 弹窗逻辑
- 不需要引入更多全局状态变量

## 个人

- 俯卧撑开始每天做几个了 + 哑铃深蹲
- 周六跑了 6km
- 帮同事写了一个简单的程序，帮助他更快的编写文档
- 到了《重构》这本书，35块钱还是挺不错
- 实现了表达式求值中对于寄存器、解引用以及逻辑与和逻辑等于的判断（虽然没有经过大量测试，但是应该问题不大）
- 尝试在 NEMU 中加入测试框架